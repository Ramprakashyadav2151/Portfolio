# Ram Prakash Yadav — Portfolio

A simple portfolio site migrated to a Vite + React app.

To run locally:

```bash
npm install
npm run dev
```

Files:
- index.html — Vite entry
- package.json — scripts and dependencies
- src/ — React source (components, styles)

Next steps:
- Provide a profile photo or resume (optional)
- Tell me any copy or color preferences
